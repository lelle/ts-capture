node_modules/
dist/
*.log
.DS_Store
.tsbuildinfo
*.tsbuildinfo
coverage/

# ts-capture's own observation artifacts (working files, not source)
.ts-capture/
types.json
types.json.applied

# Personal Claude Code settings (team-shared settings live in .claude/settings.json)
.claude/settings.local.json
